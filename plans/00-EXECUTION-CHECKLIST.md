# 00 — Порядок выполнения (рабочий чеклист агента)

> **Этот файл — живой трекер.** Агент, работающий по планам: открой этот файл, найди первый
> незакрытый этап сверху вниз, выполни его по указанному плану, **отметь чекбоксы и заполни
> строку «Статус» прямо в этом файле** в том же изменении, что и код.
>
> Правила ведения:
> - `[ ]` → `[x]` только когда пункт реально сделан и проверен, не «почти».
> - Частично сделанный этап: отметь сделанные подпункты, в «Статус» напиши, что осталось и почему
>   остановился (следующий агент продолжит с этого места).
> - Пропуск пункта по решению человека: не ставь `[x]`, пометь `[~]` и причину в «Статус».
> - Не перескакивай через этап с незакрытым блоком **Gate** (ворота верификации) — этапы упорядочены
>   по зависимостям.
> - Обнаружил, что пункт уже реализован в коде: отметь `[x]` с припиской `(уже было в коде)`.
> - **Агент НЕ коммитит и не пушит сам — никогда.** Никаких `git commit` / `git push` / `git add`:
>   по завершении этапа оставь все изменения (код + доки + этот файл) в рабочем дереве и сообщи
>   пользователю, что этап готов к ревью и коммиту. Коммитит только человек.
> - Новый этап начинай только от чистого рабочего дерева (предыдущий этап закоммичен человеком) —
>   иначе изменения этапов перемешаются в одном ревью.

**Сквозные ворота каждого этапа (Gate):** `npm run typecheck` ✅ и `npm test` ✅, обновлён
соответствующий `docs/*.md`. Изменение формы сейва без бампа `saveVersion` + `migrate()` = этап
не закрыт.

---

## Этап 0 — Верификация уже сделанного (быстрый, без кода)

По плану: `plans/README.md`, таблица «Что уже сделано».

- [x] Убедиться, что `submitLeaderboardScore` получает XP, а не balance (`gameStore.ts`, ~строка 663)
- [x] Убедиться, что `buyHint` не показывает интерстишл (только rewarded для canvass)
- [x] Убедиться, что `verdict_submit` шлёт `caseId`
- [x] Убедиться, что `claimTheses` отсутствует в `src/`
- [x] Незакоммиченные изменения из `git status` (App, StampModal, engines, persistence, store) —
      разобраться, что это за WIP, и закоммитить/согласовать до начала работ (уже было закоммичено
      в `c75f5cf` до начала этой сессии)

**Статус:** Закрыт. Все 4 пункта подтверждены чтением кода (gameStore.ts:390-457, 663, 666-684;
`grep claimTheses` — 0 совпадений). `npm run typecheck` и `npm test` (250/250) чистые на момент
старта. Рабочее дерево было чистым — WIP уже закоммичен раньше.

---

## Этап 1 — Фикс 1.5: пол награды `total ≥ 0` при верном вердикте

По плану: `plans/01-fixes.md`, этап A.

- [x] `rewardEngine.ts`: `Math.max(0, ...)` на `total`; `penalty` в breakdown остаётся фактическим
- [x] Тесты: «верный вердикт, штраф > позитива → 0»; регресс «неверный вердикт → 0 без штрафа»
- [x] Проверен вид ResultSheet при `total = 0 && penalty > 0`
- [x] `docs/04-economy-progression.md` обновлён
- [x] **Gate**: typecheck + полный `npm test`

**Статус:** Закрыт. `rewardEngine.ts:155` — `total: Math.max(0, positive + bonusComponent -
penalty)`. Тест «brute-forced» переписан на реальный отрицательный сценарий (30 ложных штампов на
easy, penalty 1500 > positive 1200 → total 0, penalty остаётся 1500); добавлен регресс-тест на
неверный вердикт. ResultSheet (`ResultSheet.tsx:282-286,489-495`) уже показывает `+0 ₽` крупно и
отдельную строку `− {penalty} ₽` в разборе — доработок не потребовалось. typecheck + `npm test`
251/251 зелёные.

---

## Этап 2 — Критичная аналитика (план 02, этап 1)

По плану: `plans/02-analytics.md`, этап 1. Делается рано — копит данные, пока идут остальные работы.

- [x] `reject_blocked` (в `handleReject`, `App.tsx`, 1/дело — реального «задизейбленного» рендера
      кнопки нет: Reject всегда кликабелен, гейт живёт в хендлере, см. `VerdictPanel.tsx` docstring)
- [x] `budget_exhausted`
- [x] `locked_case_click` (с `lockReason`, `campaignPosition`)
- [x] Агрегаты рекламы: `adsPerSession` + `verdictsSinceLastAd` (сессионные, module-level счётчики
      в `metrica.ts`, подмешаны в `ad_open`/`session_end`). `interstitialsSeenTotal` (persisted,
      кумулятивный) был отложен до бампа сейва — **сделан в Этапе 3** (`PlayerStats` +
      `recordInterstitialShown`).
- [x] Новые цели добавлены в `GOAL`-каталог и в таблицу `docs/06-yandex-platform.md`
- [~] Цели заведены в консоли Метрики — не сделано агентом (нет доступа/токена
      `YANDEX_OAUTH_TOKEN`); каталог синхронизирован (`scripts/upload-metrica-goals.mjs` обновлён,
      `npm run metrika:goals -- --list` печатает 3 новых имени без ошибки рассинхрона). **Задача
      человеку:** прогнать `npm run metrika:goals:publish` с реальным токеном перед релизом.
- [x] **Gate**: typecheck + tests

**Примечание о сейве:** если `interstitialsSeenTotal` кладётся в `PlayerStats` — НЕ бампать
saveVersion отдельно, а сделать это в Этапе 3 одним бампом 7→8. До того держать поле за миграцией
этапа 3 или временно слать только сессионные агрегаты.

**Статус:** Закрыт кроме заведения целей в консоли (человеческая задача, см. `[~]` выше).
`GOAL.rejectBlocked/budgetExhausted/lockedCaseClick` добавлены в `metrica.ts`; вызовы —
`App.tsx: handleReject` (dedup-ref на caseId), `handleOpenEvidence`, `handleSelectStandardCase`.
Ad-агрегаты реализованы централизованно внутри `trackGoal()` (перехват по имени цели), без правок
в местах вызова `ad_open`/`verdict_submit`. Новый тест в `metrica.test.ts` покрывает накопление и
сброс агрегатов. typecheck + `npm test` 252/252 зелёные.

---

## Этап 3 — Фикс 1.1: банкротство → мягкий пол (+ единый бамп сейва 7→8)

По плану: `plans/01-fixes.md`, этап B.

- [x] `gameConfig.economy.lowBalanceOfferThreshold` добавлен (500)
- [x] Все блокировки по `isBankrupt` убраны из стора и UI (гейт жил только в UI-оверлее
      `App.tsx`; стор дело при нуле и раньше стартовал — подтверждено новым store-тестом)
- [x] Жёсткий экран заменён добровольным оффером «восстановить средства за рекламу»
      (баннер на столе при `balance < 500`, закрывается ✕, ре-армится при выходе баланса
      выше порога; `restoreFunds` получил гард «не понижать баланс ≥ 2000»)
- [x] i18n ×5 для новых/переписанных строк (`bankruptTitle/Desc` → `lowBalanceTitle/Desc`)
- [x] **saveVersion 7→8**, `migrate()`: `isBankrupt: false` для сейвов v<8 + поле Этапа 2
      `interstitialsSeenTotal` персистится (`PlayerStats`, инкремент через
      `store.recordInterstitialShown()` в обеих ветках интерстишла в `App.tsx`)
- [x] Тест миграции v7→v8; store-тест «нулевой баланс не блокирует старт дела»
- [x] Goal `bankruptcy` сохранён как маркер с `blocked: false`
- [x] `docs/04` + `docs/06` обновлены (попутно исправлен дрейф `docs/01` — лидерборд
      публикует xp, не balance — и строки банкротства в `docs/03`)
- [x] **Gate**: typecheck ✅ + `npm test` 257/257 ✅; сценарий «баланс 0 → игра не
      блокируется, дело открывается, оффер закрывается» покрыт новыми UI-тестами
      `App.test.tsx (overlays)`; живой прогон в браузере/песочнице — за человеком при ревью

**Статус:** Закрыт. `isBankrupt` остался в типе как информационный маркер (аналитика),
миграция v8 разблокирует застрявших. Ручной браузерный прогон рекомендуем при ревью
(`npm run dev`, слить баланс подсказками → стол не блокируется, баннер-оффер снизу).

---

## Этап 4 — Фикс 1.7: убрать декоративный гейт уровня из UI

По плану: `plans/01-fixes.md`, этап C.

- [x] Все замки дел в UI объясняются последовательностью, не уровнем
- [x] `caseUnlockEngine.ts` и `campaignProgression.test.ts` НЕ тронуты (сортировка остаётся)
- [x] Мёртвые i18n-ключи про уровень вычищены/переписаны (×5 языков)
- [x] `docs/03-gameplay.md` обновлён
- [x] **Gate**: typecheck + tests

**Статус:** Закрыт. `formatCaseLockMessage`/`formatCaseLockTooltip` (`src/utils/caseDisplay.ts`)
теперь рендерят `requires_level` тем же текстом «Закройте предыдущее дело», что и
`complete_previous`, — уровень как причина замка нигде в UI не отображается (`CaseSelect.tsx`,
`LeftSidebar.tsx`, `MobileDeskMenu.tsx`, `App.tsx` тост — все идут через эти два форматтера, других
мест рендера `requiredLevel`/`levelsRemaining` в компонентах не найдено). `caseUnlockEngine.ts`
не тронут: `CaseUnlockReason`/`reason: 'requires_level'` остаётся внутренним полем — используется
аналитикой (`locked_case_click.lockReason`, `App.tsx`) и годится под будущий Кабинет следователя
(Этап 9). Удалены мёртвые ключи `requiresLevel`/`tipCaseLockedLevel` (были только в
`caseDisplay.ts`, ×5 языков каждый) из `UIKey`-объединения и каталога `src/i18n/ui.ts`.
`caseDisplay.test.ts` переписан под новое поведение. `docs/03-gameplay.md` обновлён.
typecheck ✅ + `npm test` 257/257 ✅.

---

## Этап 5 — Фикс 1.6: разбор «N из M» + градуированный XP за неверный вердикт

По плану: `plans/01-fixes.md`, этап D.

- [ ] ResultSheet при неверном вердикте: «найдено N из M» + упущенные противоречия с пояснениями
- [ ] `gameConfig.progression.wrongVerdict = { baseXp, proofXpShare }`; формула — в движке,
      константы — только в конфиге
- [ ] Реплеи (`rewardEligible === false`) не начали сыпать XP (регресс-тест)
- [ ] i18n ×5
- [ ] `correctStamps`/`totalContradictions` добавлены в `verdict_submit` (если не было)
- [ ] Тесты XP-градации (0 найдено → base; всё найдено → максимум)
- [ ] `docs/04` обновлён
- [ ] **Gate**: typecheck + tests

**Статус:** _не начат_

---

## Этап 6 — Фикс 1.4: выбор цели подсказки

По плану: `plans/01-fixes.md`, этап E.

- [x] `buyHint(caseData, kind, targetEvidenceId?)` + валидация + fallback на старое поведение
- [x] UI-режим прицеливания (подсветка карточек, отмена по Esc/клику мимо, крупные хит-таргеты)
- [x] Canvass: цель выбирается ДО ролика; гонка «открыл сам, пока шла реклама» обработана
- [x] i18n ×5
- [x] `hint_buy` дополнен `targeted`/`targetIndex`
- [x] Store-тесты (UI-тесты компонентов пропущены — в репо нет прецедента component-тестов
      для `CaseFile`/`EvidenceCard`, только store- и App-уровневые; сценарий покрыт store-тестами
      на выбор/фолбэк/canvass-цель + ручным прогоном)
- [x] `docs/03` обновлён (+ `docs/06` таблица целей — `hint_buy` params)
- [x] **Gate**: typecheck ✅ + `npm test` 265/265 ✅; ручной прогон — за человеком при ревью

**Статус:** Закрыт. `gameStore.buyHint` принимает необязательный `targetEvidenceId`,
валидирует принадлежность делу и «ещё не раскрыт», иначе падает обратно на «следующая по
порядку» (regression-safe для старых вызовов без цели). `CaseFile.tsx` держит режим
прицеливания в локальном state (`targeting: HintKind | null`, не в сторе/сессии — ничего не
персистится) — клик по кнопке подсказки включает режим и переключает на вкладку улик;
`EvidenceCard.targetable` рисует пульсирующую обводку рамки токеном `accent` (framer-motion,
`boxShadow` keyframes, без неонового шума); клик по подсвеченной карточке подтверждает
покупку и передаёт её id. Esc/клик мимо области улик+подсказок (`pointerdown` вне
`targetingScopeRef`) отменяет без списания. Сеалед-карточки (бюджет исчерпан) остаются
целью подсказки — хинты не тратят бюджет открытий. Canvass: цель захватывается замыканием
`reveal` до `showRewardedAd`, поэтому «открыл сам во время ролика» безопасно перекрывается
существующим guard'ом «уже раскрыто» в сторе. `hint_buy` дополнен `targeted`/`targetIndex`.
i18n: `hintTargetPrompt`/`hintTargetCancel` ×5. typecheck ✅, `npm test` 265/265 ✅. UI/browser
smoke-тест — рекомендован человеку при ревью (`npm run dev`, оба вида подсказок).

---

## Этап 7 — Остаток аналитики (план 02, этапы 2–5)

По плану: `plans/02-analytics.md`.

- [ ] Онбординг-воронка: `boot_complete` + `isFirstEver`/`msSinceBoot` в существующих целях
- [ ] `evidence_read_ms`, `result_sheet_close`, `daily_seen`, `rating_prompt_shown`, `hint_offer_seen`
- [ ] userParams: `campaignPosition`, `adsWatchedTotal`, `interstitialsSeenTotal`, `masteryGoldCount`
      (`payerFlag` — после Этапа 11)
- [ ] Техметрики: `sdk_init_failed`, `cloud_save_error`, `save_migrated`
- [ ] `docs/06` — таблица целей и userParams в локстепе
- [ ] **Gate**: typecheck + tests + dev-прогон с логирующей заглушкой `window.ym`

**Статус:** _не начат_

---

## Этап 8 — Механика 2.3: «Досье мошенников»

По плану: `plans/03-new-mechanics.md`, механика 2.3. Сейв не меняется.

- [ ] `Case.dossier?` в типах + `caseSchema.ts` (локстеп)
- [ ] `dossierEngine`/селектор: открытость и уровень карточки деривативны из `stats`
- [ ] Экран «Картотека» (i18n ×5, дизайн-язык «архивный ящик»)
- [ ] Блоки `dossier` дописаны в дела с `correctDecision: 'reject'` (можно частями — отметить долю
      в Статусе)
- [ ] Ачивки картотеки + goal `dossier_open`
- [ ] Правило авторинга в `CASE_AUTHORING_GUIDE.json` + `docs/07`; `docs/03` обновлён
- [ ] **Gate**: typecheck + tests

**Статус:** _не начат_

---

## Этап 9 — Механика 2.5: «Кабинет следователя»

По плану: `plans/03-new-mechanics.md`, механика 2.5.

- [ ] Каталог `src/data/officeItems.ts` (unlock по level/achievement; задел под purchase)
- [ ] `PlayerStats.officeSelectedBySlot` → **saveVersion 8→9** + migrate + тест
- [ ] Store-действие `selectOfficeItem` с валидацией разблокировки
- [ ] UI кабинета + слои на столе + уведомление о новом предмете при level-up (i18n ×5)
- [ ] Стартовый набор ассетов (5–6 предметов) в дизайн-языке — если арт не готов, пометить `[~]`
- [ ] Goals `office_open`, `office_item_select`
- [ ] После релиза кабинета: уровень возвращён в UI как условие предметов (закрывает долг 1.7)
- [ ] `docs/03` + `docs/05`
- [ ] **Gate**: typecheck + tests

**Статус:** _не начат_

---

## Этап 10 — Механика 2.1: «Связать улики» (прототип → контент)

По плану: `plans/03-new-mechanics.md`, механика 2.1. Только бонус, никогда штраф.

- [ ] `Case.crossLinks?` + Zod (id существуют, a ≠ b, пары уникальны)
- [ ] `ActiveSession.linkedPairs` → **saveVersion бамп** + migrate + тест
- [ ] `rewardEngine`: `linkComponent` (константы в `gameConfig.reward.crossLink`, кап, лимит попыток)
- [ ] UI-режим связывания (прототип на ОДНОМ деле, ревью UX на таче) — гейт: без одобрения
      прототипа дальше не идти
- [ ] Обучающий оверлей первого дела с crossLinks (флаг в stats — в тот же бамп)
- [ ] `crossLinks` добавлены в 4–6 экспертных дел (L14–16)
- [ ] Goal `cross_link_attempt`; `linkComponent` в `verdict_submit`
- [ ] i18n ×5; `docs/03`/`docs/04`/`docs/07` + `CASE_AUTHORING_GUIDE.json`
- [ ] **Gate**: typecheck + tests + ручной прогон дела со связями

**Статус:** _не начат_

---

## Этап 11 — IAP Путь B: «Лицензия детектива»

По плану: `plans/05-iap-path-b-license.md`. Делается раньше пути A (дешевле, инфраструктура готова).

- [ ] `PlayerStats.ownedProductIds` → saveVersion бамп (объединить с ближайшим) + migrate + тест
- [ ] Покупка + restore на буте (non-consumable, merge id, `persist(true)`)
- [ ] Эффект: без интерстишлов (`App.tsx`, обе ветки)
- [ ] Эффект: canvass без рекламы, лимит 1/дело сохранён (регресс-тест)
- [ ] Эффект: страховка стрика в `streakEngine` + `lastStreakSaveServerDay` (серверное время!)
- [ ] Эффект: эксклюзивный штамп в `collectibleStampIds`
- [ ] Витрина + контекстный оффер (порог в `gameConfig.monetization`), i18n ×5
- [ ] `payerFlag` в userParams; `license_offer_seen`
- [ ] Товар заведён в Yandex Console (задача человеку — пометить в Статусе)
- [ ] `docs/03`/`docs/04`/`docs/06`
- [ ] **Gate**: typecheck + tests + прогон в песочнице Яндекса (покупка + restore)

**Статус:** _не начат_

---

## Этап 12 — IAP Путь A: механики сезонного архива

По плану: `plans/04-iap-path-a-content.md`, фазы 1–2.

- [ ] `Case.storyline?` + Zod; гейт «эпизод N после N−1» в `caseUnlockEngine` (деривативно, сейв
      не меняется)
- [ ] Связка с досье: сквозной антагонист = серия карточек (зависит от Этапа 8)
- [ ] Капельная выдача по `getServerTimeMs()` + тизер закрытого эпизода (`locked_case_click`)
- [ ] Товары `season_archive_01` / `mini_pack_*` / `bundle_all_archives` (консоль — задача человеку)
- [ ] Покупка → `archivePurchasedPackIds` (поле есть); restore-merge на буте
- [ ] Витрина сезона (i18n ×5)
- [ ] `campaignProgression.test.ts` не зацепил архивные дела (проверить фильтр по типу)
- [ ] `docs/03`/`docs/06`/`docs/07`
- [ ] **Gate**: typecheck + tests + прогон drip-разблокировки со сдвигом серверного времени

**Статус:** _не начат_

---

## Этап 13 — IAP Путь A: контент от Codex

По плану: `plans/04-iap-path-a-content.md`, фаза 3. Можно запускать параллельно с Этапом 12,
но приёмка — только после него.

- [ ] Бриф дополнен (сеттинг сезона, имена) и отдан Codex вместе с `CASE_AUTHORING_GUIDE.json`
- [ ] Получены 10 JSON дел + season-manifest
- [ ] Все дела проходят Zod-валидацию (`npm test`)
- [ ] Вычитка ru/en человеком; проверка «противоречие находимо честно» живым прогоном каждого дела
- [ ] В сезоне есть 1–2 честных `approve`-дела; crossLinks в финальном деле работают
- [ ] Дела зарегистрированы в `caseLoader.ts` и манифесте архива
- [ ] **Gate**: typecheck + tests + полный прогон сезона от эпизода 1 до 10

**Статус:** _не начат_

---

## Этап 14 — Мобильный UX, ядро: sticky-вердикт + bottom-sheet улик (план 06, этапы A–B)

По плану: `plans/06-mobile-ux.md`, этапы A и B. Независим от этапов 5–13 — можно запускать
параллельно в любой момент; даёт ~80% ценности всего плана 06. Персист не меняется —
бампа `saveVersion` быть не должно.

- [x] **A.** Sticky нижний action-bar вердикта на мобилке (Reject/Approve + «Отмечено: N»,
      «🔎 N/M» для бюджетных), safe-area, тост `rejectNeedsProof` поднят над баром
- [x] **A.** Left/RightSidebar скрыты на мобильном экране расследования (`hidden md:block`),
      «хвоста» под вердиктом больше нет
- [x] **B.** `StampModal` на мобилке — полноэкранный bottom-sheet: шапка + sticky-футер со
      штамп-кнопкой, скроллится только тело документа
- [x] **B.** Навигация между уликами внутри просмотра: стрелки «‹ N/M ›» + свайп; переход на
      неоткрытую улику бюджетного дела идёт через гейт `markEvidenceAsViewed` (тост при отказе);
      RTL-зеркалирование
- [x] Фокус-трап/Escape/возврат фокуса модалки сохранены; десктоп-вид не изменился
- [x] i18n ×5 для новых строк; ручной прогон 390×844 (классика + бюджетное дело)
- [x] `docs/03-gameplay.md` + `docs/05-design.md` обновлены
- [ ] **Gate**: typecheck + полный `npm test`

**Статус:** Реализация закрыта; mobile smoke 390×844 подтверждает классическое и бюджетное дело.
Общий `npm test` заблокирован параллельным изменением контента: `src/data/cases/case-007.json`
содержит id `case-417` (fallback level 30, 4 улики после 6), из-за чего падают 2 проверки
`campaignProgression.test.ts`; mobile-scope 122/122 зелёные.

---

## Этап 15 — Мобильный UX, ясность: шпаргалка заявления + тач-фидбек (план 06, этапы C–D)

По плану: `plans/06-mobile-ux.md`, этапы C и D. После Этапа 14 (трогает те же компоненты).

- [x] **C.** Сворачиваемая шпаргалка заявления над сеткой улик (клиент · сумма · суть; раскрытие —
      `client.meta` + начало `claim.story`); без изменений `Case`/`caseSchema`
- [x] **D.** Approve переведён на паттерн Reject: всегда кликабелен, при блоке — причина тостом;
      то же для «Записки инспектора» при нехватке баланса
- [x] **D.** Аудит остальных мобильных Tooltip: у каждого тач-эквивалент или информация уже видна
- [x] **D.** Хаптика: `vibrate(15)` на штамп и вердикт через no-op-хелпер
- [x] Правило «никаких disabled + hover-only» зафиксировано в `docs/05-design.md`
- [ ] i18n ×5; **Gate**: typecheck + полный `npm test`

**Статус:** Реализация и typecheck закрыты; полный test gate имеет тот же внешний content-blocker
`case-007.json` → `case-417`, не связанный с мобильным UX.

---

## Этап 16 — Мобильный UX, полировка: типографика + токены (план 06, этапы E–F)

По плану: `plans/06-mobile-ux.md`, этапы E и F. Последним — конфликтует построчно с 14–15.

- [x] **E.** Типографический проход (моб. ветки): информативный текст ≥ 12–13px, кнопки вердикта
      15–16px; список правок «было → стало» в описании для ревью
- [x] **E.** «Просмотрено N/M» на классических делах; sticky-баннер прицеливания с кнопкой
      «Отмена»; свайп табов без паузы `mode="wait"`; в ResultSheet «Следующее дело» — первичная кнопка
- [x] **F.** `MobileDeskMenu.tsx` (и остатки в `CaseFile.tsx`) без hex: замена на токены,
      недостающие токены заведены в `tailwind.config.js`, визуал пиксель-в-пиксель (скриншот-diff)
- [x] Итоговая приёмка плана 06: полный прогон 390×844 (стол → классика → бюджетное → daily →
      ResultSheet → следующее дело) + тот же прогон на `ar` (RTL)
- [ ] `docs/05-design.md`: новые токены в таблице; **Gate**: typecheck + полный `npm test`

**Статус:** Реализация закрыта. Типографика: информативные mobile 9–11px → 12px,
verdict 13px → 15px, ResultSheet actions 13/14px → 15/16px. Скриншоты и JSON smoke-отчёт:
`artifacts/mobile-ux-2026-07-19/`. Typecheck, 122 mobile-scope теста и build зелёные; полный
`npm test` — 267 passed / 2 unrelated content failures (`case-417`, см. этап 14).

---

## Журнал

> Добавляй строку на каждый закрытый/приостановленный этап: дата, этап, заметки. Колонку «Коммит»
> заполняет человек после того, как сам закоммитит изменения этапа.

| Дата | Этап | Коммит | Заметки |
| --- | --- | --- | --- |
| 2026-07-18 | 0 | 8cb36b5 | Верификация уже сделанного — все 4 пункта подтверждены |
| 2026-07-18 | 1 | | Пол `total ≥ 0`; ResultSheet не потребовал правок |
| 2026-07-18 | 2 | | `reject_blocked`/`budget_exhausted`/`locked_case_click` + сессионные ad-агрегаты; заведение целей в консоли Метрики — задача человеку |
| 2026-07-18 | 1 | (следующий) | Пол `total ≥ 0`; ResultSheet не потребовал правок |
| 2026-07-18 | 3 | | Банкротство → мягкий пол; saveVersion 7→8 (+`interstitialsSeenTotal` из Этапа 2); по решению человека сделан поверх незакоммиченного Этапа 2 — в ревью пойдут вместе |
| 2026-07-19 | 4 | | Декоративный уровень убран из UI замка дел (`caseDisplay.ts`), мёртвые i18n-ключи вычищены |
| 2026-07-19 | 6 | | Сделан вне очереди по прямому запросу человека (Этап 5 остаётся открытым, следующий агент продолжает с него); выбор цели подсказки + пульсирующая подсветка карточек |
| 2026-07-19 | 14–16 | | План 06 реализован без коммита; mobile smoke RU/AR + budget/daily пройден, общий test gate заблокирован параллельным id `case-417` в `case-007.json` |
